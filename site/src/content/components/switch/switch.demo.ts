/**
 * Demo source for the Switch playground. Keeping it beside the page preserves
 * the template literal's indentation through the MDX pipeline.
 */
export default `import { Switch } from '@antadesign/anta'

<Switch label="Enable notifications" hint="Weekly digest, never marketing." />
`
